size = int(input("Enter Array Size: "))

numbers = []

for index in range(size):
    number = int(input(f"Enter Element {index + 1}: "))

    numbers.append(number)

average = sum(numbers) / len(numbers)

print("Average =", average)