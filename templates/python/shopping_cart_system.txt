records = []

while True:
    print("\n===== Shopping Cart System =====")
    print("1. Add Record")
    print("2. View All Records")
    print("3. Search Record")
    print("4. Update Record")
    print("5. Delete Record")
    print("6. Exit")

    choice = input("Please choose an option: ")

    if choice == "1":
        if len(records) >= 100:
            print("Cannot add more items. Storage is full.")
            continue
        record = {}
        record["name"] = input("Enter Product Name: ")
        record["qty"] = int(input("Enter Quantity: "))
        record["price"] = float(input("Enter Price: "))
        records.append(record)
        print("Added successfully!")

    elif choice == "2":
        if len(records) == 0:
            print("No Records Found.")
        else:
            print("\n===== Records (Product Name | Quantity | Price) =====")
            for i, record in enumerate(records):
                print(f"{i + 1}. " + str(record["name"]) + " | " + str(record["qty"]) + " | " + str(record["price"]))

    elif choice == "3":
        key = input("Enter Product Name to Search: ")
        found = False
        for record in records:
            if record["name"] == key:
                print("Found: " + str(record["name"]) + " | " + str(record["qty"]) + " | " + str(record["price"]))
                found = True
        if not found:
            print("No matching record was found.")

    elif choice == "4":
        key = input("Enter Product Name to Update: ")
        found = False
        for i in range(len(records)):
            if records[i]["name"] == key:
                records[i]["name"] = input("Enter New Product Name: ")
                records[i]["qty"] = int(input("Enter New Quantity: "))
                records[i]["price"] = float(input("Enter New Price: "))
                print("Updated successfully!")
                found = True
                break
        if not found:
            print("No matching record was found.")

    elif choice == "5":
        key = input("Enter Product Name to Delete: ")
        found = False
        for i in range(len(records)):
            if records[i]["name"] == key:
                records.pop(i)
                print("Deleted successfully!")
                found = True
                break
        if not found:
            print("No matching record was found.")

    elif choice == "6":
        print("Thank You for Using Shopping Cart System.")
        break

    else:
        print("Invalid Choice.")
