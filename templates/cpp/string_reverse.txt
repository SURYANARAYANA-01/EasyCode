#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int main() {

    string str = "EasyCode";

    reverse(str.begin(), str.end());

    cout << "Reversed String: " << str;

    return 0;
}
