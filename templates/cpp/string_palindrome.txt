#include <iostream>
#include <algorithm>

using namespace std;

int main() {

    string str = "EasyCode";
    string rev = str;

    reverse(rev.begin(), rev.end());

    if (str == rev)
        cout << "Palindrome";
    else
        cout << "Not a Palindrome";

    return 0;
}