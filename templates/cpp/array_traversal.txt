#include <iostream>
using namespace std;

int main() {
    int numbers[] = {10, 20, 30, 40, 50};

    int size = sizeof(numbers) / sizeof(numbers[0]);

    cout << "Array Elements: ";

    for (int index = 0; index < size; index++) {
        cout << numbers[index] << " ";
    }

    return 0;
}