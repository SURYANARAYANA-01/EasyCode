class Person {
    constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  greet() {
    console.log(`Hi, I'm ${this.name} and I'm ${this.age}.`);
  }
}

const name = prompt("Name:");
const age  = Number(prompt("Age:"));
new Person(name, age).greet();